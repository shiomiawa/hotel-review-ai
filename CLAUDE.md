@AGENTS.md

# CLAUDE.md

## プロジェクト概要
滞在型ホテル・旅館向けの「口コミAI分析＆返信支援アプリ」。
侍テラコヤ課題「設計から開発・公開までを自分の力でやり遂げよう」の提出物であり、転職用ポートフォリオでもある。

- 提出期限：2026年10月7日
- 公開先：Vercel
- 参考資料：docs/reference/ 内の事業計画書・事業検証レポート・戦略設計（前単元で作成）

## 解決したい課題
- 現場スタッフは口コミを見ても「返信文をどう書くか」に悩むことに意識が向き、口コミを業務改善に結び付けられていない
- 特に「大変親切な接客でした」のような一言の好評口コミへの返信に迷い、時間を取られている
- コンセプト：返信に悩む時間を、改善と接客に戻す
- **アプリの主役は「分析」（4軸分類・優先改善アラート・強み・評価点の推移）。返信下書きは、分析に時間を回すための補助機能**
  - 画面の並び順、見出し、キャッチコピーでも分析を先・大きく扱い、返信支援は後ろ・控えめに置く

## 利用者と毎日の運用
- 利用者：ホテル・旅館の支配人、フロントスタッフ
- 毎日夕方の決まった時間にアプリを開き、OTA管理画面から取り出した口コミCSVを読み込む
- ボタン1つで分析し、現場スタッフへ日次メールを送信する

## MVP機能（10/7までに作るもの）
1. 口コミCSVの読み込み（サンプルCSVを同梱）
2. 4軸への自動分類（客室設備／通信環境／接客・ホスピタリティ／朝食・ラウンジ）と、優先改善アラート・強みの抽出
3. 返信下書きの生成
   - 支配人の口調：格式高い／親しみやすい
   - 返信モード：お詫び＋再発防止／リピート誘致
   - 一言の好評口コミにも、季節感や館内サービスを絡めた人間味のある返信を作る
4. 評価点の集計：デイリー、マンスリー、前月比、前年比
5. 日次メール：当日の口コミ原文と、今月の口コミのジャンル別要約、評価点の推移を記載
6. 分析結果のCSV書き出し

## 今回は作らないもの（フェーズ2以降）
- 夜勤引継ぎ機能（故障部屋、空室状況、VIP客、重要クレームの対処経過）
- ライバル施設の料金比較
- ログイン機能、データベース、複数施設管理、料金プラン
- OTAやGoogleとのAPI連携

## データの扱い（必ず守る）
- 口コミはすべて架空のデータを使う。実在の口コミは使わない
- OTAのサイトをスクレイピング（自動収集）する機能は作らない
- 設計思想：データを取りに行くのはオーナー。アプリは渡されたCSVを分析するだけ

## サンプルCSVの列
| 列名 | 内容 |
|---|---|
| date | 投稿日（YYYY-MM-DD） |
| site | 楽天トラベル／じゃらん／Google |
| rating | 評価点（1〜5） |
| text | 口コミ本文 |
| stay_type | 宿泊タイプ（連泊／家族／カップル／ビジネス など） |

- 前年比を出すため、13か月分（2025年9月〜2026年9月）を用意する
- 一言だけの好評口コミ、長文の苦情、設備と接客が混ざった口コミなど、判断が難しい例も含める

## 技術スタック
- Next.js（Vercelで公開）
- Claude API：サーバー側（API Routes）からのみ呼び出す
- メール送信：無料枠のあるメール送信サービス（Resendなど）
- データベースは使わない（10/3時点で余裕があれば追加を検討）

## AIモデルの方針（費用はなるべく少なく）
- モデル名はコードに直接書かず、環境変数で切り替える
  - `CLAUDE_MODEL_CLASSIFY`（4軸分類・要約用）／`CLAUDE_MODEL_REPLY`（返信下書き用）
- 開発中の動作確認は、すべて Claude Haiku 4.5（`claude-haiku-4-5`）で行う
- 完成版：分類は Claude Haiku 4.5、返信下書きは Claude Sonnet 5（`claude-sonnet-5`）
- 画面ができるまでは API を呼ばず、ダミーの結果で進める（`USE_MOCK_AI=true`）
- ステップ9（効果測定）で、返信下書きを Haiku 4.5 と Sonnet 5 で比較検証する

## セキュリティのルール
- APIキーは .env.local に書き、Vercelでは環境変数に設定する
- .env.local は必ず .gitignore に入れ、GitHubに上げない
- コードにAPIキーを直接書かない
- 公開URLからの過剰な利用を防ぐため、1日の利用回数に上限を設ける

## 効果測定（レポートに使う数字）
- 返信作成時間：1件5〜7分 → 目標1分（完成後に10件実測する）
- 分類の精度：架空口コミ50件を手作業で仕分けし、AIとの一致率を出す
- APIコスト：口コミ1件あたり、1施設・月あたりの金額を記録する

## Git運用ルール（必ず守る）
- コードを変更するたびに、動作確認 → コミット → GitHubへプッシュ までを1セットで行う
  - 変更をローカルに溜めたままにしない（作業の途中で終わっても、GitHubに最新の状態が残るようにする）
- 手順：
  1. `git status` と `git diff` で変更内容を確認する
  2. 変更したファイルを指定して `git add` する（`git add .` で意図しないファイルを含めない）
  3. `git commit -m "変更内容を日本語で簡潔に"`（例：`口コミCSVの読み込み機能を追加`）
  4. `git push`
- コミット前に、.env.local やAPIキーが含まれていないことを必ず確認する
- 動作しない状態のコードはプッシュしない。途中保存が必要なときは、コミットメッセージに「作業中」と明記する
- `git push --force` や履歴の書き換えは、作業者の確認なしに行わない
- プッシュが失敗したら、原因（認証・リモート未設定・競合など）と対処法を説明してから進める

## 作業の進め方
- 作業者はプログラミング学習中なので、説明は日本語でわかりやすく行う
- 1機能ずつ作り、動作を確認してからコミット・プッシュする
- 大きな変更の前には、何をするかを先に説明して確認を取る
- エラーが出たら、原因と直し方を説明してから修正する
